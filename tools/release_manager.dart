// Maktep release manager: builds Android + Windows artifacts, uploads to
// AList over WebDAV, and updates manifest.json. See
// docs/superpowers/specs/2026-05-09-release-manager-design.md.

import 'dart:io';

void main(List<String> args) {
  stderr.writeln('release_manager: not yet implemented');
  exit(64);
}
